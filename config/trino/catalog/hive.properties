connector.name=hive
hive.metastore.uri=thrift://localhost:9083
# S3/MinIO configuration - these properties may need to be configured via environment variables
# or through a different mechanism depending on Trino version
# For now, commenting out to allow Trino to start
# hive.s3.endpoint=http://minio:9000
# hive.s3.aws-access-key=minioadmin
# hive.s3.aws-secret-key=minioadmin
# hive.s3.path-style-access=true
# hive.s3.ssl.enabled=false
